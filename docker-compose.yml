version: '3'
services:
  node:
    build: ./docker
    ports:
      - "5173:5173"
      - "6006:6006"
    volumes:
      - ".:/projects"
    environment:
      - HOST=0.0.0.0  # 外部アクセス許可
    tty: true